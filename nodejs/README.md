# SyncLite Node.js SDK

This package embeds the SyncLite Rust runtime through Node-API.

Build from the repository root with Maven, or locally from this directory:

```text
npm install
npm run build
```

The resulting `synclite_node.node` binary is bundled into the offline npm tarball staged by the platform build under `lib/nodejs`.
